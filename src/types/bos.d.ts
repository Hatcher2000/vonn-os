export interface Todo {
    id: string;
    text: string;
    completed: boolean;
    createdAt: Date;
}
export interface Todo {
    id: string;
    text: string;
    completed: boolean;
    createdAt: Date;
}
export interface Rock {
    id: string;
    title: string;
    owner: string;
    status: 'on-track' | 'off-track' | 'complete';
    dueDate: string;
    createdAt: Date;
}
//# sourceMappingURL=bos.d.ts.map